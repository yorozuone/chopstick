{% extends "/controller/admin/default.twig" %}

{% block area_body %}

    <div class="page-header">
        <h1>スタック・グループ削除</h1>
    </div>

    <ol class="breadcrumb">
        <li><a href="{{ cs_url('admin/menu') }}">Home</a></li>
        <li><a href="{{ cs_url('admin/stackgroup/summary') }}">スタック・グループ一覧</a></li>
        <li class="active">スタック・グループ削除</li>
    </ol>

    <form action="{{ cs_url('/admin/stackgroup/delete/update') }}" method="post">
        <div class="panel panel-default">
            <div class="panel-heading">スタック・グループの削除</div>
            <div class="panel-body">
                <div class="form-group">
                    <label class="control-label">スタック・グループ名</label>
                    <div>
                        <p class="form-control-static">{{ dset_stackgroup_values.caption }}</p>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-danger" value="削除">
                    <a href="{{ cs_url('/admin/stackgroup/summary') }}" class="btn btn-default">戻る</a>
                </div>
            </div>
        </div>
        <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
        <input type="hidden" name="stackgroup_id" value="{{ dset_stackgroup_values.stackgroup_id }}">
    </form>

{% endblock %}
